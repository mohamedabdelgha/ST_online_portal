import { useEffect,useState } from 'react';import { useAuth } from '../App';import { listGroups } from '../services/groupService';import { listStudents,createStudent } from '../services/studentService';import Modal from '../components/Modal';import Loading from '../components/Loading';import ErrorState from '../components/ErrorState'
export default function Students(){const {user}=useAuth();const [groups,setGroups]=useState([]);const [students,setStudents]=useState([]);const [groupId,setGroupId]=useState('all');const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [open,setOpen]=useState(false);const [form,setForm]=useState({full_name:'',phone:'',parent_phone:'',group_id:''});async function load(){try{setLoading(true);const gs=await listGroups(user.id);setGroups(gs);setStudents(await listStudents(gs.map(g=>g.id)))}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{if(user)load()},[user]);async function submit(e){e.preventDefault();try{await createStudent({...form,group_id:form.group_id?Number(form.group_id):null});setOpen(false);setForm({full_name:'',phone:'',parent_phone:'',group_id:''});load()}catch(e){setError(e.message)}}if(loading)return <Loading/>;if(error)return <ErrorState message={error} onRetry={load}/>;const visible=groupId==='all'?students:students.filter(s=>String(s.group_id)===groupId);return <div><div className="page-heading"><div><h1>Students</h1><p>View and add students in your groups.</p></div><button className="btn" onClick={()=>setOpen(true)}>+ New student</button></div><div className="toolbar"><select value={groupId} onChange={e=>setGroupId(e.target.value)}><option value="all">All groups</option>{groups.map(g=><option value={g.id} key={g.id}>{g.name}</option>)}</select></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Student</th><th>Group</th><th>Phone</th><th>Parent phone</th><th>Status</th></tr></thead><tbody>{visible.map(s=><tr key={s.id}><td>{s.full_name}</td><td>{s.groups?.name||'Unassigned'}</td><td>{s.phone||'—'}</td><td>{s.parent_phone||'—'}</td><td><span className={`badge ${s.status}`}>{s.status}</span></td></tr>)}</tbody></table></div></div>{open&&<Modal title="Add student" onClose={()=>setOpen(false)}><form onSubmit={submit} className="form-grid"><label>Full name<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} required/></label><label>Student phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Parent phone<input value={form.parent_phone} onChange={e=>setForm({...form,parent_phone:e.target.value})}/></label><label>Group<select value={form.group_id} onChange={e=>setForm({...form,group_id:e.target.value})}><option value="">Unassigned</option>{groups.map(g=><option value={g.id} key={g.id}>{g.name}</option>)}</select></label><div className="form-actions"><button className="btn secondary" type="button" onClick={()=>setOpen(false)}>Cancel</button><button className="btn">Add</button></div></form></Modal>}</div>}
