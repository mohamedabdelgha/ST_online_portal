import { useEffect,useState } from 'react';import { useAuth } from '../App';import { listGroups,createGroup } from '../services/groupService';import Modal from '../components/Modal';import Loading from '../components/Loading';import ErrorState from '../components/ErrorState'
export default function Groups(){const {user}=useAuth();const [groups,setGroups]=useState([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [open,setOpen]=useState(false);const [form,setForm]=useState({name:'',day:'Saturday',time:'17:00',max_students:20,status:'active'});async function load(){try{setLoading(true);setError('');setGroups(await listGroups(user.id))}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{if(user)load()},[user]);async function submit(e){e.preventDefault();try{await createGroup({...form,instructor_id:user.id,max_students:Number(form.max_students)});setOpen(false);setForm({name:'',day:'Saturday',time:'17:00',max_students:20,status:'active'});load()}catch(e){setError(e.message)}}if(loading)return <Loading/>;if(error)return <ErrorState message={error} onRetry={load}/>;return <div><div className="page-heading"><div><h1>My Groups</h1><p>Manage the groups assigned to you.</p></div><button className="btn" onClick={()=>setOpen(true)}>+ New group</button></div><div className="card-grid">{groups.map(g=><div className="group-card" key={g.id}><div className="group-top"><h3>{g.name}</h3><span className={`badge ${g.status}`}>{g.status}</span></div><p>{g.day} · {g.time}</p><p>Capacity: {g.max_students}</p></div>)}</div>{open&&<Modal title="Create group" onClose={()=>setOpen(false)}><form onSubmit={submit} className="form-grid"><label>Group name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Day<select value={form.day} onChange={e=>setForm({...form,day:e.target.value})}>{['Saturday','Sunday','Monday','Tuesday','Wednesday','Thursday','Friday'].map(d=><option key={d}>{d}</option>)}</select></label><label>Time<input type="time" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/></label><label>Max students<input type="number" min="1" value={form.max_students} onChange={e=>setForm({...form,max_students:e.target.value})}/></label><label>Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="active">Active</option><option value="paused">Paused</option><option value="closed">Closed</option></select></label><div className="form-actions"><button className="btn secondary" type="button" onClick={()=>setOpen(false)}>Cancel</button><button className="btn">Create</button></div></form></Modal>}</div>}
